<div class="row">
  <div class="col-md-4">
    <img class="img-responsive center-block shadow-sm p-1 bg-white rounded" src="{{user.image}}" style="width: 250px;margin-bottom: 25px;" />
  </div>
  <div class="col-md-8">

    <div class="mb-2"><strong>{{user.name}}</strong></div>
    <div class="mb-3"> {{user.email}}</div>
    <a class="btn btn-primary" href="/users/{{user.id}}/edit">Edit</a>

  </div>
  <hr />
</div>
<a href="javascript:history.back()">回上一頁</a>